import FirebaseContext, { withFirebase } from './context'
import Firebase, { Firebase as FirebaseType } from './Firebase'

export default Firebase

export { FirebaseContext, withFirebase, FirebaseType }